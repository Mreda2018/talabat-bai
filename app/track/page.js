'use client'
import {useEffect,useState} from 'react'
import {supabase} from '../lib/supabaseClient'
import {money} from '../lib/helpers'
export default function Track(){const[o,setO]=useState(null);useEffect(()=>{async function f(){const id=localStorage.getItem('belaLastOrderId');if(id){const {data}=await supabase.from('orders').select('*').eq('id',id).maybeSingle();setO(data)}}f()},[]);return <main className="app"><div className="safe"><h1>تتبع الطلب</h1><div className="track-card">{o?<><h2>{o.id}</h2><p>الإجمالي: <b>{money(o.total)}</b></p><p>الحالة: <b>{o.status}</b></p><div className="timeline"><div className="step done">✅ تم استلام الطلب</div><div className={'step '+(o.zone_review==='Approved'?'done':'')}>📍 مراجعة المنطقة</div><div className={'step '+(o.status!=='Pending Review'?'done':'')}>📍 مراجعة المنطقة</div><div className={'step '+(['تم الإرسال للتاجر','تم تأكيد التاجر','تم تحديد المندوب','تم الإرسال للمندوب','تم التوصيل'].includes(o.status)?'done':'')}>🏪 إرسال الطلب للتاجر</div><div className={'step '+(['تم تأكيد التاجر','تم تحديد المندوب','تم الإرسال للمندوب','تم التوصيل'].includes(o.status)?'done':'')}>✅ تأكيد التاجر</div><div className={'step '+(['تم تحديد المندوب','تم الإرسال للمندوب','تم التوصيل'].includes(o.status)?'done':'')}>🧾 تحديد المندوب</div><div className={'step '+(['تم الإرسال للمندوب','تم التوصيل'].includes(o.status)?'done':'')}>🛵 المندوب في الطريق</div><div className={'step '+(o.status==='تم التوصيل'?'done':'')}>📦 تم التوصيل</div></div></>:<p>لا يوجد طلبات.</p>}<button className="secondary" onClick={()=>window.location.href='/'}>العودة للرئيسية</button></div></div></main>}
