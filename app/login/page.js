'use client'
import {useState}from'react'
export default function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[err,setErr]=useState('');function l(x){x.preventDefault();if(e.trim()==='admin@talabatbela.com'&&p.trim()==='123456'){localStorage.setItem('belaAdminAuth','true');window.location.href='/admin'}else setErr('بيانات الدخول غير صحيحة')}return <main className="login-page"><form className="login-card" onSubmit={l}><h1>تسجيل دخول الإدارة</h1><input className="input" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input className="input" type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)}/><button className="primary">دخول</button>{err&&<p style={{color:'red'}}>{err}</p>}<small>admin@talabatbela.com / 123456</small></form></main>}
